const fs = require ('fs');
const data = fs.readFileSync('./productos.txt','utf-8')

class Contenedor {
    constructor(id,title,price){
        this.id = id,
        this.title = title,
        this.price = price
    }
    save(Object){
        productos.push()
    }
    getById(Number){
        productos.id.filter()
        return object 
    }
    getAll(){
        return Object [productos]
    }
    deleteById(Number){
        id.filter()
    }
    deleteAll(){
        
        object=[]
    }
} 

const datos = Contenedor(id,title,price) 

save();
getById();
getAll();
deleteById();
deleteAll();